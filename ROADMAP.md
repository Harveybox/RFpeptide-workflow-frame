# Roadmap

## GUI Refinement Backlog

- Add independent `Data Processing` filters similar to `Results Browser`, with separate target/pilot/stage/shard/search state.
- Add an embedded PDB/PyMOL-style viewer so downloaded structures can be inspected without launching an external PyMOL window.
- Move project-level settings that directly control RFDiffusion into the `RFDiffusion` page, reducing ambiguity in the `Project` page.
- Add score-item selection controls for `AfCycDesign`, including configurable metrics for candidate ranking and plotting.
- Add score-item selection controls for `PyRosetta`, including metric direction, percentile direction, and default shortlist columns.
